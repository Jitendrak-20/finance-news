{
  "name": "pulseiq",
  "version": "1.0.0",
  "private": true,
  "description": "AI-assisted financial news MVP with a Node API, static frontend, RSS ingestion, and editorial workflow.",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check app-data.js && node --check script.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@supabase/ssr": "^0.10.2",
    "@supabase/supabase-js": "^2.103.0"
  }
}
